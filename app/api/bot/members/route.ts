import { requireBotToken } from "../../../../lib/bot-auth"; import { addMember, canOperate, getMembers } from "../../../../lib/store";
export async function GET(request: Request) { const auth = await requireBotToken(request); if ("response" in auth) return auth.response; const items = await getMembers(auth.bot.organizerId); return Response.json({ members: items.map(({ id, name, email }) => ({ id, name, email })) }); }
export async function POST(request: Request) { const auth = await requireBotToken(request); if ("response" in auth) return auth.response; if (!await canOperate(auth.bot.organizerId)) return Response.json({ error: "Install inactive or household paused" }, { status: 403 }); let body: unknown; try { body = await request.json(); } catch { return Response.json({ error: "Invalid JSON" }, { status: 400 }); } const { name, email } = (body ?? {}) as Record<string, unknown>; if (typeof name !== "string" || typeof email !== "string" || !email.includes("@")) return Response.json({ error: "Valid name and email are required" }, { status: 400 }); return Response.json({ member: await addMember(auth.bot.organizerId, name, email) }, { status: 201 }); }
