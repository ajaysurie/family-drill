# Verification features

These small Gherkin files describe the user-visible evidence collected by `helpers/drive.mjs`. They deliberately share the app's seeded in-memory state and run in filename order; restart the local server to reset it. Optional docs are skipped when their route returns 404.
